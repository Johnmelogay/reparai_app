[Docs](https://docs.expo.dev/)
[Blog](https://expo.dev/blog)
[Changelog](https://expo.dev/changelog)
[Star Us on GitHub](https://github.com/expo/expo)
Search
[Home](https://docs.expo.dev/)
[Guides](https://docs.expo.dev/guides/overview)
[EAS](https://docs.expo.dev/eas)
[Reference](https://docs.expo.dev/versions/latest)
[Learn](https://docs.expo.dev/tutorial/overview)
[Overview](https://docs.expo.dev/guides/overview)
[Develop an app with Expo](https://docs.expo.dev/workflow/overview)
[Configure with app config](https://docs.expo.dev/workflow/configuration)
[Continuous Native Generation](https://docs.expo.dev/workflow/continuous-native-generation)
[Using libraries](https://docs.expo.dev/workflow/using-libraries)
[Privacy manifests](https://docs.expo.dev/guides/apple-privacy)
[Permissions](https://docs.expo.dev/guides/permissions)
[Environment variables](https://docs.expo.dev/guides/environment-variables)
Linking
Write native code
Compile locally
Web
Bundling
Existing React Native apps
Existing native apps
Reference
[Introduction](https://docs.expo.dev/router/introduction)
[Installation](https://docs.expo.dev/router/installation)
Router 101
Navigation patterns
[Stack](https://docs.expo.dev/router/advanced/stack)
[JavaScript tabs](https://docs.expo.dev/router/advanced/tabs)
[Native tabs](https://docs.expo.dev/router/advanced/native-tabs)
[Drawer](https://docs.expo.dev/router/advanced/drawer)
[Authentication](https://docs.expo.dev/router/advanced/authentication)
[Authentication (redirects)](https://docs.expo.dev/router/advanced/authentication-rewrites)
[Nesting navigators](https://docs.expo.dev/router/advanced/nesting-navigators)
[Modals](https://docs.expo.dev/router/advanced/modals)
[Web modals](https://docs.expo.dev/router/advanced/web-modals)
[Shared routes](https://docs.expo.dev/router/advanced/shared-routes)
[Protected routes](https://docs.expo.dev/router/advanced/protected)
Advanced
Web
Reference
Migration
[Overview](https://docs.expo.dev/modules/overview)
[Get started](https://docs.expo.dev/modules/get-started)
Tutorials
Reference
[Overview](https://docs.expo.dev/push-notifications/overview)
[About notification types](https://docs.expo.dev/push-notifications/what-you-need-to-know)
[Expo push notifications setup](https://docs.expo.dev/push-notifications/push-notifications-setup)
[Send notifications with the Expo Push Service](https://docs.expo.dev/push-notifications/sending-notifications)
[Handle incoming notifications](https://docs.expo.dev/push-notifications/receiving-notifications)
Reference
Analytics and error reports
Authentication
CMS
Database and SDKs
Emails
Feature flags
In-app purchases
Push notifications
Tools
TV apps
Web apps
[Upgrade Expo SDK](https://docs.expo.dev/workflow/upgrading-expo-sdk-walkthrough)
Assorted
Troubleshooting
[Data and Privacy protection](https://docs.expo.dev/regulatory-compliance/data-and-privacy-protection)
[GDPR compliance](https://docs.expo.dev/regulatory-compliance/gdpr)
[HIPAA compliance](https://docs.expo.dev/regulatory-compliance/hipaa)
[Archive](https://docs.expo.dev/archive)
[Expo Snack](https://snack.expo.dev)
[Discord and Forums](https://chat.expo.dev)
[Newsletter](https://expo.dev/mailing-list/signup)

# Native tabs

[Edit page](https://github.com/expo/expo/edit/main/docs/pages/router/advanced/native-tabs.mdx)
Edit page
Copy page
Learn how to use the native tabs layout in Expo Router.
[Edit page](https://github.com/expo/expo/edit/main/docs/pages/router/advanced/native-tabs.mdx)
Edit page
Copy page
[Liquid Glass Tabs with Expo RouterLearn how to use native tabs to create liquid glass tabs on iOS with Expo Router.](https://www.youtube.com/watch?v=QqNZXdGFl44)
Learn how to use native tabs to create liquid glass tabs on iOS with Expo Router.
Tabs are a common way to navigate between different sections of an app. In Expo Router, you can use different tab layouts, depending on your needs. This guide covers the native tabs. Unlike the [other tabs layout](https://docs.expo.dev/router/advanced/tabs#multiple-tab-layouts), native tabs use the native system tab bar.
For other tab layouts see:
[Custom tabsSee custom tabs if your app requires a fully custom design that is not possible using system tabs.](https://docs.expo.dev/router/advanced/custom-tabs)
See custom tabs if your app requires a fully custom design that is not possible using system tabs.
[JavaScript tabsSee JavaScript tabs if you already use React Navigation's tabs.](https://docs.expo.dev/router/advanced/tabs)
See JavaScript tabs if you already use React Navigation's tabs.

## https://docs.expo.dev/router/advanced/native-tabs/#get-startedGet started
You can use file-based routing to create a tabs layout. Here's an example file structure:

```
app
```


```
_layout.tsx
```


```
index.tsx
```


```
settings.tsx
```

The above file structure produces a layout with a tab bar at the bottom of the screen. The tab bar will have two tabs: Home and Settings.
You can use the app/_layout.tsx file to define your app's root layout using tabs. This file is the main layout file for the tab bar and each tab. Inside it, you can control how the tab bar and each tab item look and behave.
Copy

```
import { NativeTabs, Icon, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Label>Home</Label> <Icon sf="house.fill" drawable="custom_android_drawable" /> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Icon sf="gear" drawable="custom_settings_drawable" /> <Label>Settings</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Icon, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Label>Home</Label> <Icon sf="house.fill" drawable="custom_android_drawable" /> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Icon sf="gear" drawable="custom_settings_drawable" /> <Label>Settings</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```

Finally, you have the two tab files that make up the content of the tabs: app/index.tsx and app/settings.tsx.
Copy

```
import { View, Text, StyleSheet } from 'react-native'; export default function Tab() { return ( <View style={styles.container}> <Text>Tab [Home|Settings]</Text> </View> ); } const styles = StyleSheet.create({ container: { flex: 1, justifyContent: 'center', alignItems: 'center', }, });
```


```
import { View, Text, StyleSheet } from 'react-native'; export default function Tab() { return ( <View style={styles.container}> <Text>Tab [Home|Settings]</Text> </View> ); } const styles = StyleSheet.create({ container: { flex: 1, justifyContent: 'center', alignItems: 'center', }, });
```

The tab file named index.tsx is the default tab when the app loads. The second tab file settings.tsx shows how you can add more tabs to the tab bar.

```
NativeTabs.Trigger
```

## https://docs.expo.dev/router/advanced/native-tabs/#customizing-tab-bar-itemsCustomizing tab bar items
When you want to customize the tab bar item, we recommend using the components API designed for this purpose. Currently, you can customize:
- Icon: The icon displayed in the tab bar item.
- Label: The label displayed in the tab bar item.
- Badge: The badge displayed in the tab bar item.

### https://docs.expo.dev/router/advanced/native-tabs/#iconIcon
You can use the Icon component to customize the icon displayed in the tab bar item. The Icon component accepts a drawable prop for Android drawables, a sf prop for Apple's SF Symbols icons, or a src prop for custom images.

```
Icon
```


```
Icon
```


```
drawable
```


```
sf
```


```
src
```

Alternatively, you can pass {default: ..., selected: ...} to either the sf or src prop to specify different icons for the default and selected states.

```
{default: ..., selected: ...}
```


```
sf
```


```
src
```

To use drawable props on Android, you can use [built-in drawables](https://developer.android.com/reference/android/R.drawable) or add [custom drawables](https://developer.android.com/studio/write/resource-manager).

```
drawable
```

Copy

```
import { NativeTabs, Icon } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Icon sf={{ default: 'house', selected: 'house.fill' }} drawable="custom_home_drawable" /> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Icon src={require('../../../assets/setting_icon.png')} /> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Icon } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Icon sf={{ default: 'house', selected: 'house.fill' }} drawable="custom_home_drawable" /> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Icon src={require('../../../assets/setting_icon.png')} /> </NativeTabs.Trigger> </NativeTabs> ); }
```

Liquid glass on iOS automatically changes colors based on if the background color is light or dark. There is no callback for this, so you need to use a PlatformColor to set the color of the icon.

```
PlatformColor
```

Copy

```
import { DynamicColorIOS } from 'react-native'; import { NativeTabs, Icon } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs labelStyle={{ // For the text color color: DynamicColorIOS({ dark: 'white', light: 'black', }), }} // For the selected icon color tintColor={DynamicColorIOS({ dark: 'white', light: 'black', })}> <NativeTabs.Trigger name="index"> <Icon sf={{ default: 'house', selected: 'house.fill' }} drawable="custom_home_drawable" /> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Icon src={{ default: require('../../../assets/setting_icon.png'), selected: require('../../../assets/selected_setting_icon.png'), }} /> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { DynamicColorIOS } from 'react-native'; import { NativeTabs, Icon } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs labelStyle={{ // For the text color color: DynamicColorIOS({ dark: 'white', light: 'black', }), }} // For the selected icon color tintColor={DynamicColorIOS({ dark: 'white', light: 'black', })}> <NativeTabs.Trigger name="index"> <Icon sf={{ default: 'house', selected: 'house.fill' }} drawable="custom_home_drawable" /> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Icon src={{ default: require('../../../assets/setting_icon.png'), selected: require('../../../assets/selected_setting_icon.png'), }} /> </NativeTabs.Trigger> </NativeTabs> ); }
```

### https://docs.expo.dev/router/advanced/native-tabs/#labelLabel
You can use the Label component to customize the label displayed in the tab bar item. The Label component accepts a string label passed as a child. If no label is provided, the tab bar item will use the route name as the label.

```
Label
```


```
Label
```

If you don't want to display a label, you can use the hidden prop to hide the label.

```
hidden
```

Copy

```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Label hidden /> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Label hidden /> </NativeTabs.Trigger> </NativeTabs> ); }
```

### https://docs.expo.dev/router/advanced/native-tabs/#badgeBadge

You can use the Badge component to customize the badge displayed for the tab bar item. The badge is an additional mark on top of the tab and useful for showing notification or unread message counts.

```
Badge
```

Copy

```
import { NativeTabs, Badge } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="messages"> <Badge>9+</Badge> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Badge /> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Badge } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="messages"> <Badge>9+</Badge> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Badge /> </NativeTabs.Trigger> </NativeTabs> ); }
```

## https://docs.expo.dev/router/advanced/native-tabs/#customizing-the-tab-barCustomizing the tab bar

Since the native tab layout's appearance varies by platform, the customization options are also different. For all customization options, see the [API reference for NativeTabs](https://docs.expo.dev/versions/latest/sdk/router-native-tabs).

```
NativeTabs
```

### https://docs.expo.dev/router/advanced/native-tabs/#hide-a-tab-conditionallyHide a tab conditionally
If you want to hide a tab based on a condition, you can either remove the trigger or pass the hidden prop to the NativeTabs.Trigger component.

```
hidden
```


```
NativeTabs.Trigger
```

Copy

```
import { NativeTabs } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { const shouldHideMessagesTab = true; // Replace with your condition return ( <NativeTabs> <NativeTabs.Trigger name="messages" hidden={shouldHideMessagesTab} /> </NativeTabs> ); }
```


```
import { NativeTabs } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { const shouldHideMessagesTab = true; // Replace with your condition return ( <NativeTabs> <NativeTabs.Trigger name="messages" hidden={shouldHideMessagesTab} /> </NativeTabs> ); }
```


```
hidden
```

### https://docs.expo.dev/router/advanced/native-tabs/#dismiss-behaviorDismiss behavior
By default, tapping a tab that is already active closes all screens in that tab's stack and returns to the root screen. You can disable this by setting the disablePopToTop prop on the NativeTabs.Trigger component.

```
disablePopToTop
```


```
NativeTabs.Trigger
```

Copy

```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index" disablePopToTop> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Label>Settings</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index" disablePopToTop> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Label>Settings</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```

### https://docs.expo.dev/router/advanced/native-tabs/#scroll-to-topScroll to top
By default, tapping a tab that is already active and showing its root screen scrolls the content back to the top. You can disable this by setting the disableScrollToTop prop on the NativeTabs.Trigger component.

```
disableScrollToTop
```


```
NativeTabs.Trigger
```

Copy

```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index" disableScrollToTop> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Label>Settings</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index" disableScrollToTop> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="settings"> <Label>Settings</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```

### https://docs.expo.dev/router/advanced/native-tabs/#ios-26-featuresiOS 26 features

#### https://docs.expo.dev/router/advanced/native-tabs/#separate-search-tabSeparate search tab
To add a separate search tab, assign the role with its value set to search to the native tab you want to display separately.

```
role
```


```
search
```

Copy

```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="search" role="search"> <Label>Search</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="search" role="search"> <Label>Search</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```


#### https://docs.expo.dev/router/advanced/native-tabs/#tabbar-search-inputTabbar search input
To add a search field to the tab bar, wrap the screen in a Stack navigator and configure headerSearchBarOptions.

```
headerSearchBarOptions
```


```
app
```


```
_layout.tsx
```


```
index.tsx
```


```
search
```


```
_layout.tsx
```


```
index.tsx
```

Copy

```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="search" role="search"> <Label>Search</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="search" role="search"> <Label>Search</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```

Copy

```
import { Stack } from 'expo-router'; export default function SearchLayout() { return ( <Stack> <Stack.Screen name="index" options={{ title: 'Search', headerSearchBarOptions: { placement: 'automatic', placeholder: 'Search', onChangeText: () => {}, }, }} /> </Stack> ); }
```


```
import { Stack } from 'expo-router'; export default function SearchLayout() { return ( <Stack> <Stack.Screen name="index" options={{ title: 'Search', headerSearchBarOptions: { placement: 'automatic', placeholder: 'Search', onChangeText: () => {}, }, }} /> </Stack> ); }
```

Copy

```
import { ScrollView } from 'react-native'; export default function SearchIndex() { return <ScrollView>{/* Screen content */}</ScrollView>; }
```


```
import { ScrollView } from 'react-native'; export default function SearchIndex() { return <ScrollView>{/* Screen content */}</ScrollView>; }
```


#### https://docs.expo.dev/router/advanced/native-tabs/#tab-bar-minimize-behaviorTab bar minimize behavior
To implement the minimized behavior on the tab bar, you can use [minimizeBehavior](https://docs.expo.dev/versions/latest/sdk/router-native-tabs#minimizebehavior) prop on NativeTabs. In the example below, the tab bar is minimized when scrolling down.

```
minimizeBehavior
```


```
NativeTabs
```

Copy

```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs minimizeBehavior="onScrollDown"> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="tab-1"> <Label>Tab 1</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs minimizeBehavior="onScrollDown"> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> <NativeTabs.Trigger name="tab-1"> <Label>Tab 1</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```

### https://docs.expo.dev/router/advanced/native-tabs/#integration-with-expovector-iconsIntegration with @expo/vector-icons
```
@expo/vector-icons
```

[SF Symbols on iOS](https://docs.expo.dev/router/advanced/native-tabs#icon)
To use icons from @expo/vector-icons, you can use VectorIcon component.

```
@expo/vector-icons
```


```
VectorIcon
```

Copy

```
import MaterialIcons from '@expo/vector-icons/MaterialIcons'; import { NativeTabs, Icon, VectorIcon } from 'expo-router/unstable-native-tabs'; import { Platform } from 'react-native'; export default function TabLayout() { return ( <NativeTabs minimizeBehavior="onScrollDown"> <NativeTabs.Trigger name="index"> <Label>Home</Label> {Platform.select({ ios: <Icon sf="house.fill" />, android: <Icon src={<VectorIcon family={MaterialIcons} name="home" />} />, })} </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import MaterialIcons from '@expo/vector-icons/MaterialIcons'; import { NativeTabs, Icon, VectorIcon } from 'expo-router/unstable-native-tabs'; import { Platform } from 'react-native'; export default function TabLayout() { return ( <NativeTabs minimizeBehavior="onScrollDown"> <NativeTabs.Trigger name="index"> <Label>Home</Label> {Platform.select({ ios: <Icon sf="house.fill" />, android: <Icon src={<VectorIcon family={MaterialIcons} name="home" />} />, })} </NativeTabs.Trigger> </NativeTabs> ); }
```

### https://docs.expo.dev/router/advanced/native-tabs/#disabling-keyboard-avoidance-on-androidDisabling keyboard avoidance on Android
By default, when the keyboard is displayed on Android, the native tabs automatically adjust to avoid being obscured. You can disable this behavior by changing the [android.softwareKeyboardLayoutMode](https://docs.expo.dev/versions/latest/config/app#softwarekeyboardlayoutmode) property to pan in your app config file:

```
android.softwareKeyboardLayoutMode
```


```
pan
```

Copy

```
{ "expo": { "android": { "softwareKeyboardLayoutMode": "pan" } } }
```


```
{ "expo": { "android": { "softwareKeyboardLayoutMode": "pan" } } }
```

## https://docs.expo.dev/router/advanced/native-tabs/#migrating-from-javascript-tabsMigrating from JavaScript tabs

Native tabs are not designed to be a drop-in replacement for [JavaScript tabs](https://docs.expo.dev/router/advanced/tabs). The native tabs are constrained to the native platform behavior, whereas the JavaScript tabs can be customized more freely. If you aren't interested in the native platform behavior, you can continue using the JavaScript tabs.

### https://docs.expo.dev/router/advanced/native-tabs/#use-trigger-instead-of-screenUse Trigger instead of Screen
```
Trigger
```


```
Screen
```

NativeTabs introduces the concept of a Trigger for adding routes to a layout. Unlike a Screen, which styles routes that are added automatically, the Trigger system gives you better control for hiding and removing tabs from the tab bar.

```
NativeTabs
```


```
Trigger
```


```
Screen
```


```
Trigger
```

### https://docs.expo.dev/router/advanced/native-tabs/#use-react-components-instead-of-propsUse React components instead of props
NativeTabs has a React-first API that opts to use components for defining UI in favor of props objects.

```
NativeTabs
```

### https://docs.expo.dev/router/advanced/native-tabs/#use-stacks-inside-tabsUse Stacks inside tabs
The JavaScript <Tabs /> have a mock stack header which is not present in the native tabs. Instead, you should nest a native <Stack /> layout inside the native tabs to support both headers and pushing screens.

```
<Tabs />
```


```
<Stack />
```

### https://docs.expo.dev/router/advanced/native-tabs/#a-limit-of-5-tabs-on-androidA limit of 5 tabs on Android
On Android, there is a limitation of having a maximum of 5 tabs in the tab bar. This restriction comes from the platform's Material Tabs component.

### https://docs.expo.dev/router/advanced/native-tabs/#cannot-measure-the-tab-bar-heightCannot measure the tab bar height
The tabs move around, sometimes being on top of the screen when rendering on iPad, sometimes on the side of the screen when running on Apple Vision Pro, and so on. We're working on a layout function to provide more detailed layout info in the future.

### https://docs.expo.dev/router/advanced/native-tabs/#no-support-for-nested-native-tabsNo support for nested native tabs
Native tabs cannot be nested inside other native tabs. You can still nest [JavaScript tabs](https://docs.expo.dev/router/advanced/tabs) inside native tabs.

### https://docs.expo.dev/router/advanced/native-tabs/#limited-support-for-flatlistLimited support for FlatList

[FlatList](https://reactnative.dev/docs/flatlist) integration with native tabs has limitations. Features like scroll-to-top and minimize-on-scroll aren't supported. Additionally, detecting scroll edges may fail, causing the tab bar to appear transparent. To fix this, use the [disableTransparentOnScrollEdge](https://docs.expo.dev/versions/latest/sdk/router-native-tabs#disabletransparentonscrolledge) prop.

```
disableTransparentOnScrollEdge
```

Copy

```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs disableTransparentOnScrollEdge> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```


```
import { NativeTabs, Label } from 'expo-router/unstable-native-tabs'; export default function TabLayout() { return ( <NativeTabs disableTransparentOnScrollEdge> <NativeTabs.Trigger name="index"> <Label>Home</Label> </NativeTabs.Trigger> </NativeTabs> ); }
```

[Previous (Expo Router - Navigation patterns)JavaScript tabs](https://docs.expo.dev/router/advanced/tabs)
Previous (Expo Router - Navigation patterns)
JavaScript tabs
[Next (Expo Router - Navigation patterns)Drawer](https://docs.expo.dev/router/advanced/drawer)
Next (Expo Router - Navigation patterns)
Drawer
Was this doc helpful?
- [Share your feedback](https://docs.expo.dev/router/advanced/native-tabs/)
- [Ask a question on the forums](https://chat.expo.dev/)
- [Edit this page](https://github.com/expo/expo/edit/main/docs/pages/router/advanced/native-tabs.mdx)
- Last updated on December 30, 2025
[Share your feedback](https://docs.expo.dev/router/advanced/native-tabs/)
Share your feedback
[Ask a question on the forums](https://chat.expo.dev/)
Ask a question on the forums
[Edit this page](https://github.com/expo/expo/edit/main/docs/pages/router/advanced/native-tabs.mdx)
Edit this page
Sign up for the Expo Newsletter
Unsubscribe at any time. Read our [privacy policy](https://expo.dev/privacy).
On this page
[Get started](https://docs.expo.dev/router/advanced/native-tabs/#get-started)
Get started
[Customizing tab bar items](https://docs.expo.dev/router/advanced/native-tabs/#customizing-tab-bar-items)
Customizing tab bar items
[Icon](https://docs.expo.dev/router/advanced/native-tabs/#icon)
Icon
[Label](https://docs.expo.dev/router/advanced/native-tabs/#label)
Label
[Badge](https://docs.expo.dev/router/advanced/native-tabs/#badge)
Badge
[Customizing the tab bar](https://docs.expo.dev/router/advanced/native-tabs/#customizing-the-tab-bar)
Customizing the tab bar
[Advanced](https://docs.expo.dev/router/advanced/native-tabs/#advanced)
Advanced
[Hide a tab conditionally](https://docs.expo.dev/router/advanced/native-tabs/#hide-a-tab-conditionally)
Hide a tab conditionally
[Dismiss behavior](https://docs.expo.dev/router/advanced/native-tabs/#dismiss-behavior)
Dismiss behavior
[Scroll to top](https://docs.expo.dev/router/advanced/native-tabs/#scroll-to-top)
Scroll to top
[iOS 26 features](https://docs.expo.dev/router/advanced/native-tabs/#ios-26-features)
iOS 26 features
[Integration with  @expo/vector-icons](https://docs.expo.dev/router/advanced/native-tabs/#integration-with-expovector-icons)
Integration with @expo/vector-icons
[Disabling keyboard avoidance on Android](https://docs.expo.dev/router/advanced/native-tabs/#disabling-keyboard-avoidance-on-android)
Disabling keyboard avoidance on Android
[Migrating from JavaScript tabs](https://docs.expo.dev/router/advanced/native-tabs/#migrating-from-javascript-tabs)
Migrating from JavaScript tabs
[Use  Trigger  instead of  Screen](https://docs.expo.dev/router/advanced/native-tabs/#use-trigger-instead-of-screen)
Use Trigger instead of Screen
[Use React components instead of props](https://docs.expo.dev/router/advanced/native-tabs/#use-react-components-instead-of-props)
Use React components instead of props
[Use Stacks inside tabs](https://docs.expo.dev/router/advanced/native-tabs/#use-stacks-inside-tabs)
Use Stacks inside tabs
[Known limitations](https://docs.expo.dev/router/advanced/native-tabs/#known-limitations)
Known limitations
[A limit of 5 tabs on Android](https://docs.expo.dev/router/advanced/native-tabs/#a-limit-of-5-tabs-on-android)
A limit of 5 tabs on Android
[Cannot measure the tab bar height](https://docs.expo.dev/router/advanced/native-tabs/#cannot-measure-the-tab-bar-height)
Cannot measure the tab bar height

### https://docs.expo.dev/router/advanced/native-tabs/#limited-support-for-flatlistLimited support for FlatList

[No support for nested native tabs](https://docs.expo.dev/router/advanced/native-tabs/#no-support-for-nested-native-tabs)
No support for nested native tabs
[Limited support for FlatList](https://docs.expo.dev/router/advanced/native-tabs/#limited-support-for-flatlist)
Limited support for FlatList